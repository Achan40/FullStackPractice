//In javascript "2" == 2 will return True
//We fix this by adding another equals sign, ===
// == checks for value, not datatype

//JS has automatic type conversion

// null == underfined will evaluate to true
// NaN == NaN will evaluate to false

// And
1 === 1 && 2 === 2;

//OR
1 === 3 || 1 === 2;

//NOT, checking the opposite of a condition
!(1===1)